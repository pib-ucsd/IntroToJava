ds_queue_enqueue(global.consoleQueue, "Quick, the portal is open for only 8 more seconds");
ds_queue_enqueue(global.consoleQueue, "Hey Loady, look, a water portal!");
ds_queue_enqueue(global.consoleQueue, "Nice job laddy, but we should get going");
ds_queue_enqueue(global.consoleQueue, "Hey Loady, check out my cool socks! They have llamas!");
ds_queue_enqueue(global.consoleQueue, "Quick, the portal is open for only 4 more seconds");
ds_queue_enqueue(global.consoleQueue, "Quick, the portal is open for only 3 more seconds");
ds_queue_enqueue(global.consoleQueue, "Quick, the portal is open for only 2 more seconds");
ds_queue_enqueue(global.consoleQueue, "Quick, the portal is open for only 1 more seconds");
ds_queue_enqueue(global.consoleQueue, "Quick, the portal is open for only 0 more seconds");